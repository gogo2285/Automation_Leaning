package polymorphism;

public class Testoverride2 extends override {
	
	public Integer sum() /// so u can write Integer also instead of int as these are wrapper class
	
	{
		return  10;
	}
	
	//Webdriver driver = new FirefoxDriver(); this is a polumorphic reference in which you can save object of an child class in the reference variable of parent class
 
	//reverse is not possible
	
	// polymorfism is only possible with inheritence
}
